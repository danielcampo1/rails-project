<!DOCTYPE html>
<html>
  <head>
    <title>RailsProject</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
    <%= link_to "Home", root_path %>
  <nav>
      <% if logged_in? %>
        <%= link_to "Logout", logout_path, method: "delete" %>  <%= link_to "Add destination", new_user_destination_path(current_user) %>    <%= link_to "check out where others have traveled", countries_path %>
      <% else %>
        <%= link_to "Sign in", signin_path %> <%= link_to "Sign up", new_user_path %>
        <% end %>
    </nav>

  <div>
   <% if !!flash[:errors] %>
  <% flash[:errors].each do |e| %>
    <%= e %> <br>
    <% end %>
  <% end %>
   </div>

  <body>
    <%= yield %>
  </body>
</html>
